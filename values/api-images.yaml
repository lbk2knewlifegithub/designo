tag: dev

# Api Images
apiStorage:
  name: api-images
  replicaCount: 2
  image: okteto.dev/api-images
  serviceType: ClusterIP
  port: 8081
  storage:
    size: 5Gi
    storageMountPath: /app/static
