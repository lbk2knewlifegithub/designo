import { createAction } from '@ngrx/store';

/**
 * - Show Required Login
 */
export const showRequiredLogin = createAction(
  '[Core/Product Feedbacks] Show Required Login'
);

/**
 * - Close Required Login
 */
export const closeRequiredLogin = createAction(
  '[Core/Product Feedbacks] Close Required Login'
);
