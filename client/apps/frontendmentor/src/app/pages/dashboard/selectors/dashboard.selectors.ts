import { dashboardFeature } from '../reducers';

export const {
  selectChallenges,
  selectCredits,
  selectFollowers,
  selectFollowing,
  selectLoading,
  selectLoaded,
  selectError,
  selectBookmarks,
  selectTags,
  selectTitle,
} = dashboardFeature;
