deploy:
  # Build Client Invoice App
  - okteto build -t okteto.dev/client-invoice-app:${OKTETO_GIT_COMMIT} client -f client/invoice-app.Dockerfile
  # Ungrade Chart
  - helm upgrade -i client-invoice-app charts/client -n client-lbk2knewlifegithub -f values/client-invoice-app.yaml --set tag=${OKTETO_GIT_COMMIT}
