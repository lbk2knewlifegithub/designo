deploy:
  # Build Client Audiophile
  - okteto build -t okteto.dev/client-audiophile:${OKTETO_GIT_COMMIT} client -f client/audiophile.Dockerfile
  # Ungrade Chart
  - helm upgrade -i client-audiophile charts/client -n client-lbk2knewlifegithub -f values/client-audiophile.yaml --set tag=${OKTETO_GIT_COMMIT}
